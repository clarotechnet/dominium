# Adicionar ao docker/Caddyfile do stack dominium-toa no servidor:

{$DOMINIUM_DOMAIN} {
    encode zstd gzip
    reverse_proxy host.docker.internal:8791 {
        header_up X-Dominium-Proxy-Token {$DOMINIUM_PROXY_TOKEN}
    }
}
